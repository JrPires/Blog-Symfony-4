{%  extends 'base.html.twig' %}
{% block body %}
    {% if title is defined %}
    <h1>{{ title }}</h1>
    {%  endif %}

    {# lista de postagens #}
        <ul>
    {% for post in posts %}
        <li>{{ post.id}}  - {{ post.title }}  --
            {{ post.created_at|date("d/m/Y H:i:s") }}</li>
    {% endfor %}
        </ul>
    {# lista de postagens #}


{% endblock body %}
{% block title %}
Homepage
{% endblock %}