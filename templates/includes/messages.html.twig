{% for messege in app.session.flashbag.get('success') %}
    <div class="alert alert-success">
        {{ message }}
    </div>
{% endfor %}

{% for messege in app.session.flashbag.get('warning') %}
    <div class="alert alert-warning">
        {{ message }}
    </div>
{% endfor %}

{% for messege in app.session.flashbag.get('errors') %}
    <div class="alert alert-danger">
        {{ message }}
    </div>
{% endfor %}